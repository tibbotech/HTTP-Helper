include "global.tbh"


'dim url as string = "txt2html.sourceforge.net/sample.txt"
'dim post_data as string = "name1=value1&name2=value2"


dim url as string = "httpbin.org/get"
dim data as string = "name1=value1&name2=value2"


sub on_sys_init()
	
	DHCP_DNS_init()
	dhcp_start(PL_SOCK_INTERFACE_NET,"","")

end sub

sub on_button_pressed()

	http_send_request(url, HTTP_GET, PL_SOCK_INTERFACE_NET, data)
'	send_get_request(url, PL_SOCK_INTERFACE_NET)
	
end sub